# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build fitfriends-back`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t fitfriends-back`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system fitfriends-back && \
          adduser --system -G fitfriends-back fitfriends-back

COPY dist/apps/fitfriends-back fitfriends-back/
RUN chown -R fitfriends-back:fitfriends-back .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix fitfriends-back --omit=dev -f install

CMD [ "node", "fitfriends-back" ]
